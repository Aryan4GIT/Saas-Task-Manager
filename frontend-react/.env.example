# Prefer relative URL in dev so Vite proxy (/api -> backend) works and avoids CORS issues.
# For production, set this to your backend URL, e.g. https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1
